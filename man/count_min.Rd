% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/count-min.R
\name{count_min}
\alias{count_min}
\title{Count-Min sketch for approximate point-frequency estimation}
\usage{
count_min(
  x = NULL,
  weight = NULL,
  num_hashes = NULL,
  num_buckets = NULL,
  seed = NULL,
  bytes = NULL
)
}
\arguments{
\item{x}{Optional numeric or character vector to update the new sketch
with.}

\item{weight}{Optional weight(s) for \code{x}: a single finite number (recycled,
may be negative or fractional), or a vector of such values matching the
length of \code{x}. Defaults to \code{1} (each occurrence counts once). Cannot be
set without \code{x}.}

\item{num_hashes}{Number of hash functions, a single whole number in
\verb{[1, 255]}. Larger values increase confidence but also memory use.
Defaults to \code{3}. Must not be set when \code{bytes} is supplied. See
\code{\link[=count_min_suggest_num_hashes]{count_min_suggest_num_hashes()}}.}

\item{num_buckets}{Number of buckets per hash function, a single whole
number of at least \code{3} (and such that \code{num_buckets * num_hashes < 2^30}).
Larger values are more accurate and larger. Defaults to \code{55}. Must not be
set when \code{bytes} is supplied. See \code{\link[=count_min_suggest_num_buckets]{count_min_suggest_num_buckets()}}.}

\item{seed}{Hash seed, a single non-negative whole number up to \code{2^53}.
Defaults to \code{9001}. Two sketches can only be merged if their \code{seed} (and
\code{num_hashes} and \code{num_buckets}) match.}

\item{bytes}{Optional \link{raw} vector holding a native serialized sketch to
reconstruct.}
}
\value{
A \code{count_min_sketch} object. Key methods:
\describe{
\item{\verb{$update(x, weight = NULL)}}{Add numeric or character values with
an optional weight (mutates, returns the sketch).}
\item{\verb{$merge(other)}}{Absorb another sketch with matching
\code{num_hashes}, \code{num_buckets}, and \code{seed} (mutates, returns the
sketch).}
\item{\verb{$estimate(item)}, \verb{$lower_bound(item)}, \verb{$upper_bound(item)}}{
Estimated frequency and guaranteed bounds for one or more items.}
\item{\verb{$total_weight()}, \verb{$relative_error()}, \verb{$num_hashes()},
\verb{$num_buckets()}, \verb{$seed()}, \verb{$is_empty()}}{Metadata accessors.}
\item{\verb{$summary()}, \verb{$inspect()}, \verb{$serialize()}}{Structured metadata,
verbose debug output, and the native byte payload.}
}
}
\description{
Creates a \href{https://apache.github.io/datasketches-cpp/5.2.0/classdatasketches_1_1count__min__sketch.html}{Count-Min}
sketch, a mergeable summary that estimates the frequency (sum of weights)
of individual items in a numeric or character stream far larger than
memory, with one-sided error: \verb{$estimate()} never under-estimates the true
frequency.
}
\details{
At most one of \code{x} or \code{bytes} may be supplied:
\itemize{
\item Pass \code{x} to build a sketch and immediately update it with a numeric or
character vector (optionally with \code{weight}).
\item Pass \code{bytes} to reconstruct a sketch from a native serialized payload (as
produced by \code{sketch$serialize()}). \code{num_hashes} and \code{num_buckets} are
restored from the payload and must not be supplied alongside \code{bytes}.
\item Pass neither for an empty sketch with the given \code{num_hashes} and
\code{num_buckets}.
}

Numeric items are hashed via the raw bytes of their IEEE-754 double
representation; this is internally consistent between \code{update()} and the
\code{estimate()}/\code{lower_bound()}/\code{upper_bound()} queries, but is not guaranteed
to match hashes produced by other DataSketches language implementations for
the same numeric value.

\code{NA}/\code{NaN}/\code{NA_character_} are silently ignored by \code{update()}, matching the
missing-value policy used across families; there is no \code{na_rm} argument.

Two sketches can only be \verb{$merge()}d if they share the same \code{num_hashes},
\code{num_buckets}, and \code{seed}; a mismatch raises
\code{datasketches_incompatible_sketch}.
}
\examples{
words <- sample(letters[1:5], 1000, replace = TRUE, prob = c(.5, .25, .1, .1, .05))
sketch <- count_min(words)
sketch$estimate("a")
sketch$relative_error()

# Round-trip through the native byte format.
restored <- count_min(bytes = sketch$serialize())
identical(restored$total_weight(), sketch$total_weight())

}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filters-bloom.R
\name{bloom_filter}
\alias{bloom_filter}
\title{Bloom filter for approximate set membership}
\usage{
bloom_filter(
  x = NULL,
  max_items = NULL,
  fpp = NULL,
  num_bits = NULL,
  num_hashes = NULL,
  seed = NULL,
  bytes = NULL
)
}
\arguments{
\item{x}{Optional numeric or character vector of items to update the new
filter with.}

\item{max_items}{Target maximum number of distinct items, a single positive
whole number up to \code{2^53}. Must be supplied together with \code{fpp}, and
cannot be combined with \code{num_bits}/\code{num_hashes}. Must not be set when
\code{bytes} is supplied.}

\item{fpp}{Target false-positive probability, a single number in \verb{(0, 1]}.
Must be supplied together with \code{max_items}. Must not be set when \code{bytes}
is supplied.}

\item{num_bits}{Number of bits in the filter, a single positive whole number
up to \code{2^53}. Must be supplied together with \code{num_hashes}, and cannot be
combined with \code{max_items}/\code{fpp}. Must not be set when \code{bytes} is supplied.}

\item{num_hashes}{Number of hash functions applied per item, a single whole
number in \verb{[1, 65535]}. Must be supplied together with \code{num_bits}. Must
not be set when \code{bytes} is supplied.}

\item{seed}{Hash seed, a single non-negative whole number up to \code{2^53}.
Defaults to \code{9001}. Two filters can only be combined if their \code{seed} (and
\code{num_hashes} and \code{capacity}) match. Must not be set when \code{bytes} is
supplied.}

\item{bytes}{Optional \link{raw} vector holding a native serialized filter to
reconstruct.}
}
\value{
A \code{bloom_filter} object. Key methods:
\describe{
\item{\verb{$update(x)}}{Add items (mutates, returns the filter).}
\item{\verb{$query(x)}}{Logical vector: might each element have been seen?}
\item{\verb{$query_and_update(x)}}{\verb{$query()} against the prior state,
then \verb{$update()} (mutates, returns the query result).}
\item{\verb{$merge(other)}}{In-place logical OR with a compatible filter
(mutates, returns the filter).}
\item{\verb{$intersect(other)}}{In-place logical AND with a compatible
filter (mutates, returns the filter).}
\item{\verb{$invert()}}{In-place logical NOT (mutates, returns the
filter).}
\item{\verb{$reset()}}{Clear all bits, keeping sizing and \code{seed} (mutates,
returns the filter).}
\item{\verb{$is_compatible(other)}}{Whether \code{other} may be combined with
this filter.}
\item{\verb{$capacity()}, \verb{$num_hashes()}, \verb{$seed()}, \verb{$bits_used()},
\verb{$is_empty()}}{Metadata accessors.}
\item{\verb{$summary()}, \verb{$inspect()}, \verb{$serialize()}}{Structured metadata,
verbose debug output, and the native byte payload.}
}
}
\description{
Creates a
\href{https://github.com/apache/datasketches-cpp/tree/master/filters}{Bloom filter},
a probabilistic data structure for approximate set membership. Querying an
item that has been added always returns \code{TRUE} (no false negatives);
querying an item that has never been added may return \code{TRUE} with
probability up to the configured false-positive probability.
}
\details{
Unlike the other sketch families, a Bloom filter is not sub-linear in size:
it is sized up front and does not resize itself. There are two sizing
strategies, which cannot be combined:
\itemize{
\item \code{max_items} and \code{fpp} size the filter for a target number of distinct
items and a target false-positive probability.
\item \code{num_bits} and \code{num_hashes} size the filter explicitly.
}

If neither strategy is specified, the filter defaults to
\code{max_items = 10000} and \code{fpp = 0.01}.

At most one of \code{x} or \code{bytes} may be supplied:
\itemize{
\item Pass \code{x} to build a filter and immediately update it with a numeric or
character vector of items.
\item Pass \code{bytes} to reconstruct a filter from a native serialized payload (as
produced by \code{filter$serialize()}). \code{max_items}, \code{fpp}, \code{num_bits},
\code{num_hashes}, and \code{seed} must not be supplied alongside \code{bytes}; they are
restored from the payload.
\item Pass neither for an empty (mutable) filter with the given sizing.
}

\code{update()}, \code{query()}, and \code{query_and_update()} silently ignore (or return
\code{NA} for) \code{NA}/\code{NaN}/\code{NA_character_} in \code{x}, matching the missing-value
policy used across families.

Two filters can only be combined with \verb{$merge()} (logical OR) or
\verb{$intersect()} (logical AND) if they are "compatible": they share the same
\code{seed}, \code{num_hashes}, and \code{capacity} (a mismatch raises
\code{datasketches_incompatible_sketch}).
}
\examples{
bf <- bloom_filter(letters, max_items = 1000, fpp = 0.01)
bf$query(c("a", "z", "!"))

# Round-trip through the native byte format.
restored <- bloom_filter(bytes = bf$serialize())
restored$query("a")

}

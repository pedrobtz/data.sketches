% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cardinality-theta.R
\name{theta_set_operations}
\alias{theta_set_operations}
\alias{theta_union}
\alias{theta_intersection}
\alias{theta_difference}
\alias{theta_jaccard}
\title{Theta sketch set operations}
\usage{
theta_union(a, b, lg_k = NULL)

theta_intersection(a, b)

theta_difference(a, b)

theta_jaccard(a, b)
}
\arguments{
\item{a, b}{\code{theta_sketch} objects created with the same \code{seed}.}

\item{lg_k}{For \code{theta_union()}, log2 of the nominal number of entries for
the union's internal sketch, a single whole number in \verb{[5, 26]}. Defaults
to the larger of \code{a} and \code{b}'s configured \code{lg_k} (or \code{12} for compact
inputs).}
}
\value{
A compact \code{theta_sketch} object (\code{theta_union()},
\code{theta_intersection()}, \code{theta_difference()}), or a named numeric vector
\code{c(lower_bound, estimate, upper_bound)} (\code{theta_jaccard()}).
}
\description{
Combine two \code{\link[=theta]{theta()}} sketches into a new compact \code{theta_sketch} result,
without mutating either input. \code{a} and \code{b} must be Theta sketches created
with the same \code{seed} (a mismatch raises \code{datasketches_seed_mismatch}).
}
\details{
\itemize{
\item \code{theta_union(a, b)} estimates the size of the union \code{union(A, B)}.
\item \code{theta_intersection(a, b)} estimates the size of the intersection
\code{intersection(A, B)}.
\item \code{theta_difference(a, b)} estimates the size of the set difference
\verb{A \\\\ B} (elements in \code{A} but not \code{B}).
\item \code{theta_jaccard(a, b)} estimates the
\href{https://en.wikipedia.org/wiki/Jaccard_index}{Jaccard similarity index}
\verb{J(A, B) = |intersection(A, B)| / |union(A, B)|}, returning a named
numeric vector
\code{c(lower_bound, estimate, upper_bound)} for a ~95\% confidence interval.
}
}
\examples{
a <- theta(1:1000)
b <- theta(501:1500)
theta_union(a, b)$estimate()
theta_intersection(a, b)$estimate()
theta_difference(a, b)$estimate()
theta_jaccard(a, b)

}

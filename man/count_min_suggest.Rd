% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/count-min.R
\name{count_min_suggest}
\alias{count_min_suggest}
\alias{count_min_suggest_num_buckets}
\alias{count_min_suggest_num_hashes}
\title{Suggest Count-Min sketch parameters}
\usage{
count_min_suggest_num_buckets(relative_error)

count_min_suggest_num_hashes(confidence)
}
\arguments{
\item{relative_error}{Desired relative error, a single positive number.
\code{count_min_suggest_num_buckets()} returns the smallest \code{num_buckets} such
that the sketch's \verb{$relative_error()} does not exceed this value.}

\item{confidence}{Desired confidence, a single number in \verb{(0, 1]}.
\code{count_min_suggest_num_hashes()} returns the smallest \code{num_hashes} such
that, with this probability, \verb{$estimate()} is within \verb{$relative_error()}
of the true frequency.}
}
\value{
A single integer.
}
\description{
Helpers to translate a desired accuracy into the \code{num_buckets} and
\code{num_hashes} arguments of \code{\link[=count_min]{count_min()}}.
}
\examples{
num_buckets <- count_min_suggest_num_buckets(0.05)
num_hashes <- count_min_suggest_num_hashes(0.95)
sketch <- count_min(num_hashes = num_hashes, num_buckets = num_buckets)

}

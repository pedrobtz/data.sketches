% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filters-bloom.R
\name{bloom_filter_suggest}
\alias{bloom_filter_suggest}
\alias{bloom_filter_suggest_num_filter_bits}
\alias{bloom_filter_suggest_num_hashes}
\title{Suggest Bloom filter sizing parameters}
\usage{
bloom_filter_suggest_num_filter_bits(max_items, fpp)

bloom_filter_suggest_num_hashes(max_items, num_bits)
}
\arguments{
\item{max_items}{Target maximum number of distinct items, a single positive
whole number up to \code{2^53}.}

\item{fpp}{Target false-positive probability, a single number in \verb{(0, 1]}.}

\item{num_bits}{Number of bits in the filter, a single positive whole number
up to \code{2^53}.}
}
\value{
A single number: \code{bloom_filter_suggest_num_filter_bits()} returns
the suggested \code{num_bits} (a double, which may exceed
\code{.Machine$integer.max}); \code{bloom_filter_suggest_num_hashes()} returns the
suggested \code{num_hashes} (an integer).
}
\description{
Helpers that translate a target accuracy into Bloom filter constructor
arguments for the \code{num_bits}/\code{num_hashes} sizing strategy. These compute
the same values that \code{\link[=bloom_filter]{bloom_filter()}} uses internally for the
\code{max_items}/\code{fpp} sizing strategy, for callers who want to inspect or reuse
them (for example, to create multiple compatible filters with an explicit
\code{seed}).
}
\examples{
num_bits <- bloom_filter_suggest_num_filter_bits(1000, 0.01)
num_hashes <- bloom_filter_suggest_num_hashes(1000, num_bits)
bf <- bloom_filter(num_bits = num_bits, num_hashes = num_hashes)

}

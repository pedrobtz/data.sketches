% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/frequent-items.R
\name{frequent_items}
\alias{frequent_items}
\title{Frequent Items sketch for approximate frequency estimation}
\usage{
frequent_items(
  x = NULL,
  weight = NULL,
  lg_max_map_size = NULL,
  lg_start_map_size = NULL,
  bytes = NULL
)
}
\arguments{
\item{x}{Optional character vector to update the new sketch with.}

\item{weight}{Optional weight(s) for \code{x}: a single non-negative whole
number (recycled), or a vector of such values matching the length of
\code{x}. Defaults to \code{1} (each occurrence counts once). Cannot be set without
\code{x}.}

\item{lg_max_map_size}{log2 of the maximum size of the internal hash map, a
single whole number in \verb{[3, 30]}. Larger values are more accurate and
larger. Defaults to \code{8}. Must not be set when \code{bytes} is supplied.}

\item{lg_start_map_size}{log2 of the starting size of the internal hash
map, a single whole number in \verb{[3, lg_max_map_size]}. Defaults to \code{3}.
Must not be set when \code{bytes} is supplied.}

\item{bytes}{Optional \link{raw} vector holding a native serialized sketch to
reconstruct.}
}
\value{
A \code{frequent_items_sketch} object. Key methods:
\describe{
\item{\verb{$update(x, weight = NULL)}}{Add character values with an
optional weight (mutates, returns the sketch).}
\item{\verb{$merge(other)}}{Absorb another sketch (mutates, returns the
sketch).}
\item{\verb{$estimate(item)}, \verb{$lower_bound(item)}, \verb{$upper_bound(item)}}{
Estimated frequency and guaranteed bounds for one or more items.}
\item{\verb{$frequent_items(error_type = "no_false_positives", threshold = NULL)}}{
A data frame of items whose estimated frequency exceeds \code{threshold}
(defaults to \verb{$maximum_error()}), with columns \code{item}, \code{estimate},
\code{lower_bound}, and \code{upper_bound}.}
\item{\verb{$maximum_error()}, \verb{$epsilon()}, \verb{$total_weight()},
\verb{$num_active_items()}, \verb{$is_empty()}}{Metadata accessors.}
\item{\verb{$summary()}, \verb{$inspect()}, \verb{$serialize()}}{Structured metadata,
verbose debug output, and the native byte payload.}
}
}
\description{
Creates a \href{https://datasketches.apache.org/docs/Frequency/FrequencySketches.html}{Frequent Items}
sketch, a mergeable summary that estimates the frequencies of the most
frequent items in a character stream far larger than memory, with
guaranteed error bounds.
}
\details{
At most one of \code{x} or \code{bytes} may be supplied:
\itemize{
\item Pass \code{x} to build a sketch and immediately update it with a character
vector (optionally with \code{weight}).
\item Pass \code{bytes} to reconstruct a sketch from a native serialized payload (as
produced by \code{sketch$serialize()}). \code{lg_max_map_size} and
\code{lg_start_map_size} are restored from the payload and must not be
supplied alongside \code{bytes}.
\item Pass neither for an empty sketch with the given \code{lg_max_map_size} and
\code{lg_start_map_size}.
}

\code{NA_character_} is silently ignored by \code{update()}, matching the
missing-value policy used across families; there is no \code{na_rm} argument.
}
\examples{
words <- sample(letters[1:5], 1000, replace = TRUE, prob = c(.5, .25, .1, .1, .05))
sketch <- frequent_items(words)
sketch$frequent_items()
sketch$estimate("a")

# Round-trip through the native byte format.
restored <- frequent_items(bytes = sketch$serialize())
identical(restored$total_weight(), sketch$total_weight())

}

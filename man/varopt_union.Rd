% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sampling-varopt.R
\name{varopt_union}
\alias{varopt_union}
\title{Combine two VarOpt sketches}
\usage{
varopt_union(a, b)
}
\arguments{
\item{a, b}{\code{varopt_sketch} objects holding the same item type.}
}
\value{
A \code{varopt_sketch} object.
}
\description{
Combines two \code{\link[=varopt]{varopt()}} sketches into a new \code{varopt_sketch} result,
without mutating either input. \code{a} and \code{b} must hold the same item type (a
mismatch raises \code{datasketches_incompatible_sketch}). The result is sized
for the larger of \code{a} and \code{b}'s configured \code{k}.
}
\examples{
a <- varopt(1:1000, runif(1000), k = 50)
b <- varopt(501:1500, runif(1000), k = 50)
u <- varopt_union(a, b)
u$k()
u$n()

}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tuple.R
\name{array_of_doubles_set_operations}
\alias{array_of_doubles_set_operations}
\alias{array_of_doubles_union}
\alias{array_of_doubles_intersection}
\alias{array_of_doubles_difference}
\title{Array of Doubles sketch set operations}
\usage{
array_of_doubles_union(a, b, lg_k = NULL)

array_of_doubles_intersection(a, b)

array_of_doubles_difference(a, b)
}
\arguments{
\item{a, b}{\code{array_of_doubles_sketch} objects created with the same \code{seed}.}

\item{lg_k}{For \code{array_of_doubles_union()}, log2 of the nominal number of
entries for the union's internal sketch, a single whole number in
\verb{[5, 26]}. Defaults to the larger of \code{a} and \code{b}'s configured \code{lg_k} (or
\code{12} for compact inputs).}
}
\value{
A compact \code{array_of_doubles_sketch} object.
}
\description{
Combine two \code{\link[=array_of_doubles]{array_of_doubles()}} sketches into a new compact
\code{array_of_doubles_sketch} result, without mutating either input. \code{a} and
\code{b} must be Array of Doubles sketches created with the same \code{seed} (a
mismatch raises \code{datasketches_seed_mismatch}).
}
\details{
\itemize{
\item \code{array_of_doubles_union(a, b)} estimates the size of the union
\code{union(A, B)}. \code{a} and \code{b} must also share the same \code{num_values} (a
mismatch raises \code{datasketches_incompatible_sketch}); value arrays for
matching keys are combined by element-wise sum.
\item \code{array_of_doubles_intersection(a, b)} estimates the size of the
intersection \code{intersection(A, B)}, with the same \code{num_values}
requirement and combining rule as \code{array_of_doubles_union()}.
\item \code{array_of_doubles_difference(a, b)} estimates the size of the set
difference \verb{A \\\\ B} (elements in \code{A} but not \code{B}), retaining \code{a}'s value
arrays unchanged for the retained keys.
}
}
\examples{
a <- array_of_doubles(1:1000, runif(1000))
b <- array_of_doubles(501:1500, runif(1000))
array_of_doubles_union(a, b)$column_sums()
array_of_doubles_intersection(a, b)$estimate()
array_of_doubles_difference(a, b)$estimate()

}

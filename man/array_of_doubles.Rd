% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tuple.R
\name{array_of_doubles}
\alias{array_of_doubles}
\title{Array of Doubles (Tuple) sketch for estimating sums alongside distinct counts}
\usage{
array_of_doubles(
  x = NULL,
  values = NULL,
  lg_k = NULL,
  num_values = NULL,
  seed = NULL,
  bytes = NULL
)
}
\arguments{
\item{x}{Optional numeric or character vector of keys to update the new
sketch with. Each element is hashed and contributes to the
distinct-count estimate.}

\item{values}{Optional value(s) associated with each element of \code{x}: a
numeric vector (when \code{num_values == 1}) or a numeric matrix with
\code{num_values} columns, recycled to \code{length(x)} rows if a single
value/row is supplied. Defaults to \code{1}s (so \verb{$column_sums()} estimates
the count of each key, like \verb{$estimate()}). Cannot be set without \code{x}.}

\item{lg_k}{log2 of the nominal number of entries, a single whole number in
\verb{[5, 26]}. Larger \code{lg_k} is more accurate and larger. Defaults to \code{12}
(resolved when a fresh sketch is built). Must not be set when \code{bytes} is
supplied.}

\item{num_values}{Number of double values associated with each retained
key, a single whole number in \verb{[1, 255]}. Defaults to \code{1}. Must not be
set when \code{bytes} is supplied.}

\item{seed}{Hash seed, a single non-negative whole number up to \code{2^53}.
Defaults to \code{9001} (the upstream default), resolved whether or not \code{bytes}
is supplied.}

\item{bytes}{Optional \link{raw} vector holding a native serialized sketch to
reconstruct. The result is always a compact sketch.}
}
\value{
An \code{array_of_doubles_sketch} object. Key methods:
\describe{
\item{\verb{$update(x, values = NULL)}}{Add keys with associated values
(mutates, returns the sketch). Errors if the sketch is compact.}
\item{\verb{$merge(other)}}{Absorb another sketch with the same \code{seed} and
\code{num_values}, becoming compact (mutates, returns the sketch).}
\item{\verb{$estimate()}}{Approximate number of distinct keys seen.}
\item{\verb{$lower_bound(num_std_dev = 1)} / \verb{$upper_bound(num_std_dev = 1)}}{
Approximate confidence bounds on \code{estimate()}, at 1, 2, or 3 standard
deviations.}
\item{\verb{$column_sums()}}{Estimated sum of each value column over the
full input stream.}
\item{\verb{$lg_k()}, \verb{$num_values()}, \verb{$seed()}, \verb{$theta()},
\verb{$num_retained()}, \verb{$is_empty()}, \verb{$is_estimation_mode()},
\verb{$is_ordered()}, \verb{$is_compact()}}{Metadata accessors.}
\item{\verb{$summary()}, \verb{$inspect()}, \verb{$serialize()}}{Structured metadata,
verbose debug output, and the native byte payload.}
}
}
\description{
Creates an
\href{https://datasketches.apache.org/docs/Tuple/TupleOverview.html}{Array of Doubles}
sketch, a Tuple sketch that extends a \code{\link[=theta]{theta()}} sketch by associating a
fixed-length array of \code{num_values} doubles with each retained key. It
estimates not only the number of distinct keys (\verb{$estimate()}, as for
\code{\link[=theta]{theta()}}) but also the sum of each value column over the full input
stream (\verb{$column_sums()}), e.g. to estimate the total of a numeric measure
across distinct users.
}
\details{
At most one of \code{x} or \code{bytes} may be supplied:
\itemize{
\item Pass \code{x} to build a sketch and immediately update it with a numeric or
character vector of keys (optionally with \code{values}).
\item Pass \code{bytes} to reconstruct a sketch from a native serialized payload (as
produced by \code{sketch$serialize()}). The result is always a \emph{compact}
sketch (see below); \code{lg_k} and \code{num_values} must not be supplied
alongside \code{bytes}. Unlike \code{lg_k}, the hash \code{seed} is \emph{not} stored in the
payload and must be supplied if the original sketch did not use the
default.
\item Pass neither for an empty (mutable) sketch with the given \code{lg_k},
\code{num_values}, and \code{seed}.
}

\code{update()} silently ignores \code{NA}/\code{NaN}/\code{NA_character_} in \code{x} (and the
corresponding row of \code{values}), matching the missing-value policy used
across families; there is no \code{na_rm} argument.

An Array of Doubles sketch is either an \emph{update} sketch (mutable,
\verb{$is_compact()} is \code{FALSE}) or a \emph{compact} sketch (immutable,
\verb{$is_compact()} is \code{TRUE}). Fresh sketches built from \code{x}/\code{lg_k} are update
sketches and can be grown with \verb{$update()}. Compact sketches arise from
\verb{bytes =} reconstruction, \verb{$merge()}, or any of the
\verb{array_of_doubles_*()} set operations, and cannot be updated further.
\verb{$lg_k()} is only defined for update sketches.

Two sketches can only be merged with \verb{$merge()}, or combined with
\code{array_of_doubles_union()} / \code{array_of_doubles_intersection()}, if they
share the same \code{seed} (a mismatch raises \code{datasketches_seed_mismatch}) and
the same \code{num_values} (a mismatch raises
\code{datasketches_incompatible_sketch}). Value arrays for matching keys are
combined by element-wise sum. \verb{$merge()} mutates the receiver into a
compact sketch holding the union of both inputs (so it can no longer be
\verb{$update()}d afterward).
}
\examples{
keys <- sample(1000, 5000, replace = TRUE)
values <- runif(length(keys))
sketch <- array_of_doubles(keys, values)
sketch$estimate()
sketch$column_sums()

# Round-trip through the native byte format (always compact).
restored <- array_of_doubles(bytes = sketch$serialize())
restored$is_compact()
identical(restored$column_sums(), sketch$column_sums())

}

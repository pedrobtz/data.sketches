% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sampling-reservoir.R
\name{ebpps}
\alias{ebpps}
\title{EBPPS sketch for proportional-to-size sampling}
\usage{
ebpps(x = NULL, weight = NULL, k = NULL, type = NULL, bytes = NULL)
}
\arguments{
\item{x}{Optional numeric or character vector of items to update the new
sketch with.}

\item{weight}{Optional weight(s) for each element of \code{x}: a single
non-negative, finite number, or a vector of such values matching
\code{length(x)}. Defaults to \code{1}. Cannot be set without \code{x}.}

\item{k}{Maximum sample size, a single whole number in \verb{[1, 2^31 - 2]}.
Defaults to \code{256}. Must not be set when \code{bytes} is supplied.}

\item{type}{Item type for a fresh sketch, either \code{"double"} or
\code{"character"}. Defaults to the type of \code{x} (or \code{"double"} if \code{x} is not
supplied). Must not be set when \code{bytes} is supplied.}

\item{bytes}{Optional \link{raw} vector holding a native serialized sketch to
reconstruct.}
}
\value{
An \code{ebpps_sketch} object. Key methods:
\describe{
\item{\verb{$update(x, weight = NULL)}}{Add weighted items (mutates, returns
the sketch).}
\item{\verb{$merge(other)}}{Absorb another sketch with the same item type
(mutates, returns the sketch).}
\item{\verb{$result()}}{The current sample as a numeric or character
vector.}
\item{\verb{$k()}, \verb{$n()}, \verb{$cumulative_weight()}, \verb{$c()}, \verb{$is_empty()},
\verb{$is_character()}}{Metadata accessors.}
\item{\verb{$summary()}, \verb{$inspect()}, \verb{$serialize()}}{Structured metadata,
verbose debug output, and the native byte payload.}
}
}
\description{
Creates an
\href{https://datasketches.apache.org/docs/Sampling/EB-PPS_SamplingSketches.html}{EBPPS}
(Exact and Bounded Probabilistic Proportional-to-Size) sketch, which
samples up to \code{k} items from a stream of weighted (item, weight) pairs. It
is a modern alternative to classic reservoir sampling: each item's
inclusion probability is proportional to its share of the total stream
weight, with a tight bound on the resulting sample size.
}
\details{
Unlike the hash-based cardinality and frequency sketches, EBPPS retains
items verbatim rather than hashing them, so the item type (numeric or
character) is fixed when the sketch is created and cannot change.

At most one of \code{x} or \code{bytes} may be supplied:
\itemize{
\item Pass \code{x} to build a sketch and immediately update it with a numeric or
character vector of items (optionally with \code{weight}). The item type is
inferred from \code{x} unless \code{type} is supplied.
\item Pass \code{bytes} to reconstruct a sketch from a native serialized payload (as
produced by \code{sketch$serialize()}). \code{weight}, \code{k}, and \code{type} must not be
supplied alongside \code{bytes}; they are restored from the payload.
\item Pass neither for an empty (mutable) sketch with the given \code{k} and \code{type}.
}

\code{update()} silently ignores \code{NA}/\code{NaN}/\code{NA_character_} in \code{x} (and the
corresponding \code{weight}), matching the missing-value policy used across
families; there is no \code{na_rm} argument.

Two sketches can only be merged with \verb{$merge()} if they hold the same item
type (a mismatch raises \code{datasketches_incompatible_sketch}). The merged
sketch is resized to the smaller of the two inputs' configured \code{k},
matching the native implementation.
}
\examples{
items <- 1:1000
weights <- runif(1000)
sketch <- ebpps(items, weights, k = 50)
sketch$result()
sketch$c()

# Round-trip through the native byte format.
restored <- ebpps(bytes = sketch$serialize())
restored$k()

}

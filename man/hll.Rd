% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cardinality-hll.R
\name{hll}
\alias{hll}
\title{HLL sketch for approximate distinct counting}
\usage{
hll(x = NULL, lg_k = NULL, type = NULL, bytes = NULL)
}
\arguments{
\item{x}{Optional numeric or character vector to update the new sketch
with. Each element is hashed and contributes to the distinct-count
estimate.}

\item{lg_k}{log2 of the number of buckets, a single whole number in
\verb{[4, 21]}. Larger \code{lg_k} is more accurate and larger. Defaults to \code{12}
(resolved when a fresh sketch is built). Must not be set when \code{bytes} is
supplied.}

\item{type}{One of \code{"HLL_4"}, \code{"HLL_6"}, or \code{"HLL_8"}, controlling the
per-bucket encoding width (a size/speed trade-off that does not affect
accuracy). Defaults to \code{"HLL_4"} (resolved when a fresh sketch is built).
Must not be set when \code{bytes} is supplied.}

\item{bytes}{Optional \link{raw} vector holding a native serialized sketch to
reconstruct.}
}
\value{
An \code{hll_sketch} object. Key methods:
\describe{
\item{\verb{$update(x)}}{Add numeric or character values (mutates, returns
the sketch).}
\item{\verb{$merge(other)}}{Absorb another sketch (mutates, returns the
sketch).}
\item{\verb{$estimate()}}{Approximate number of distinct values seen.}
\item{\verb{$lower_bound(num_std_dev = 1)} / \verb{$upper_bound(num_std_dev = 1)}}{
Approximate confidence bounds on \code{estimate()}, at 1, 2, or 3 standard
deviations.}
\item{\verb{$lg_k()}, \verb{$hll_type()}, \verb{$is_empty()}, \verb{$is_compact()}}{
Metadata accessors.}
\item{\verb{$summary()}, \verb{$inspect()}, \verb{$serialize()}}{Structured metadata,
verbose debug output, and the native byte payload.}
}
}
\description{
Creates an \href{https://datasketches.apache.org/docs/HLL/HllSketches.html}{HLL}
(HyperLogLog) sketch, a compact, mergeable summary that estimates the
number of distinct values seen in a stream far larger than memory.
}
\details{
At most one of \code{x} or \code{bytes} may be supplied:
\itemize{
\item Pass \code{x} to build a sketch and immediately update it with a numeric or
character vector.
\item Pass \code{bytes} to reconstruct a sketch from a native serialized payload (as
produced by \code{sketch$serialize()}). Configuration is restored from the
payload, so \code{lg_k} and \code{type} must not be supplied alongside \code{bytes}.
\item Pass neither for an empty sketch with the given \code{lg_k} and \code{type}.
}

\code{update()} silently ignores \code{NA}/\code{NaN}/\code{NA_character_}, matching the
missing-value policy used across families; there is no \code{na_rm} argument.
}
\examples{
sketch <- hll(sample(1000, 5000, replace = TRUE))
sketch$estimate()
sketch$lower_bound()
sketch$upper_bound()

# Round-trip through the native byte format.
restored <- hll(bytes = sketch$serialize())
identical(restored$estimate(), sketch$estimate())

}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quantiles-kll-floats.R
\name{kll_floats}
\alias{kll_floats}
\title{KLL sketch for approximate quantiles of a numeric stream stored as floats}
\usage{
kll_floats(x = NULL, k = NULL, bytes = NULL)
}
\arguments{
\item{x}{Optional numeric vector to update the new sketch with.}

\item{k}{Sketch width controlling the accuracy/size trade-off, a whole number
in \verb{[8, 65535]}. Larger \code{k} is more accurate and larger. Defaults to \code{200}
(resolved when a fresh sketch is built). Must not be set when \code{bytes} is
supplied.}

\item{bytes}{Optional \link{raw} vector holding a native serialized sketch to
reconstruct.}
}
\value{
A \code{kll_floats_sketch} object. Key methods:
\describe{
\item{\verb{$update(x)}}{Add numeric values (mutates, returns the sketch).}
\item{\verb{$merge(other)}}{Absorb another sketch (mutates, returns the sketch).}
\item{\verb{$quantile(probs, inclusive = TRUE)}}{Approximate quantiles for
probabilities in \verb{[0, 1]}.}
\item{\verb{$rank(x, inclusive = TRUE)}}{Approximate ranks of \code{x}; missing
inputs return \code{NA}.}
\item{\verb{$cdf(split_points)} / \verb{$pmf(split_points)}}{Cumulative / mass
estimates; return \code{length(split_points) + 1} values.}
\item{\verb{$n()}, \verb{$k()}, \verb{$num_retained()}, \verb{$is_empty()},
\verb{$is_estimation_mode()}, \verb{$min()}, \verb{$max()}, \verb{$rank_error(pmf = FALSE)}}{
Metadata and accuracy accessors.}
\item{\verb{$summary()}, \verb{$inspect()}, \verb{$serialize()}}{Structured metadata,
verbose debug output, and the native byte payload.}
}
}
\description{
Creates a \href{https://datasketches.apache.org/docs/KLL/KLLSketch.html}{KLL}
quantile sketch over \code{float} (32-bit) values. A KLL sketch is a compact,
mergeable summary that answers approximate quantile, rank, CDF, and PMF
queries over a stream far larger than memory, with a configurable
accuracy/size trade-off controlled by \code{k}.
}
\details{
Retained items are stored as native 32-bit \code{float}, not R's 64-bit
\code{double}. Updates and query results are rounded to float precision; use
\code{\link[=kll_doubles]{kll_doubles()}} when full double precision is required.

At most one of \code{x} or \code{bytes} may be supplied:
\itemize{
\item Pass \code{x} to build a sketch and immediately update it with a numeric vector.
\item Pass \code{bytes} to reconstruct a sketch from a native serialized payload (as
produced by \code{sketch$serialize()}). The width is restored from the payload,
so \code{k} must not be supplied alongside \code{bytes}.
\item Pass neither for an empty sketch of width \code{k}.
}

\code{update()} silently ignores \code{NA}/\code{NaN}, matching the upstream/Python behaviour;
there is no \code{na_rm} argument.
}
\examples{
sketch <- kll_floats(rnorm(10000))
sketch$quantile(c(0.25, 0.5, 0.75))
sketch$rank(c(-1, 0, 1))

# Round-trip through the native byte format.
restored <- kll_floats(bytes = sketch$serialize())
identical(restored$quantile(0.5), sketch$quantile(0.5))

}

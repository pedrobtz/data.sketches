% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quantiles-tdigest-double.R
\name{tdigest_double}
\alias{tdigest_double}
\title{t-Digest sketch for approximate quantiles of a numeric stream}
\usage{
tdigest_double(x = NULL, k = NULL, bytes = NULL)
}
\arguments{
\item{x}{Optional numeric vector to update the new sketch with.}

\item{k}{Compression parameter controlling the accuracy/size trade-off, a
whole number in \verb{[10, 65535]}. Larger \code{k} is more accurate and larger.
Defaults to \code{200} (resolved when a fresh sketch is built). Must not be set
when \code{bytes} is supplied.}

\item{bytes}{Optional \link{raw} vector holding a native serialized sketch to
reconstruct.}
}
\value{
A \code{tdigest_double_sketch} object. Key methods:
\describe{
\item{\verb{$update(x)}}{Add numeric values (mutates, returns the sketch).}
\item{\verb{$merge(other)}}{Absorb another sketch (mutates, returns the sketch).}
\item{\verb{$quantile(probs)}}{Approximate quantiles for probabilities in
\verb{[0, 1]}.}
\item{\verb{$rank(x)}}{Approximate ranks of \code{x}; missing inputs return \code{NA}.}
\item{\verb{$cdf(split_points)} / \verb{$pmf(split_points)}}{Cumulative / mass
estimates; return \code{length(split_points) + 1} values.}
\item{\verb{$n()}, \verb{$k()}, \verb{$is_empty()}, \verb{$min()}, \verb{$max()}}{Metadata
accessors.}
\item{\verb{$summary()}, \verb{$inspect()}, \verb{$serialize()}}{Structured metadata,
verbose debug output, and the native byte payload.}
}
}
\description{
Creates a \href{https://github.com/tdunning/t-digest}{t-Digest} quantile sketch
over \code{double} values. A t-Digest is a compact, mergeable summary that
answers approximate quantile, rank, CDF, and PMF queries over a stream far
larger than memory. Compared to \code{\link[=kll_doubles]{kll_doubles()}} and \code{\link[=req]{req()}}, a t-Digest
concentrates its accuracy near the tails of the distribution (extreme
quantiles such as p99 or p99.9), at some cost to accuracy near the median.
}
\details{
At most one of \code{x} or \code{bytes} may be supplied:
\itemize{
\item Pass \code{x} to build a sketch and immediately update it with a numeric vector.
\item Pass \code{bytes} to reconstruct a sketch from a native serialized payload (as
produced by \code{sketch$serialize()}). The width is restored from the payload,
so \code{k} must not be supplied alongside \code{bytes}.
\item Pass neither for an empty sketch of width \code{k}.
}

\code{update()} silently ignores \code{NA}/\code{NaN}, matching the upstream/Python behaviour;
there is no \code{na_rm} argument.

Unlike \code{\link[=kll_doubles]{kll_doubles()}} and \code{\link[=req]{req()}}, \verb{$quantile()} and \verb{$rank()} have no
\code{inclusive} argument, and there is no \verb{$rank_error()} accuracy accessor.
}
\examples{
sketch <- tdigest_double(rnorm(10000))
sketch$quantile(c(0.5, 0.99, 0.999))
sketch$rank(c(-1, 0, 1))

# Round-trip through the native byte format.
restored <- tdigest_double(bytes = sketch$serialize())
identical(restored$quantile(0.5), sketch$quantile(0.5))

}

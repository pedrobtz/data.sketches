% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cardinality-theta.R
\name{theta}
\alias{theta}
\title{Theta sketch for approximate distinct counting and set operations}
\usage{
theta(x = NULL, lg_k = NULL, seed = NULL, bytes = NULL)
}
\arguments{
\item{x}{Optional numeric or character vector to update the new sketch
with. Each element is hashed and contributes to the distinct-count
estimate.}

\item{lg_k}{log2 of the nominal number of entries, a single whole number in
\verb{[5, 26]}. Larger \code{lg_k} is more accurate and larger. Defaults to \code{12}
(resolved when a fresh sketch is built). Must not be set when \code{bytes} is
supplied.}

\item{seed}{Hash seed, a single non-negative whole number up to \code{2^53}.
Defaults to \code{9001} (the upstream default), resolved whether or not \code{bytes}
is supplied.}

\item{bytes}{Optional \link{raw} vector holding a native serialized sketch to
reconstruct. The result is always a compact sketch.}
}
\value{
A \code{theta_sketch} object. Key methods:
\describe{
\item{\verb{$update(x)}}{Add numeric or character values (mutates, returns
the sketch). Errors if the sketch is compact.}
\item{\verb{$merge(other)}}{Absorb another sketch with the same \code{seed},
becoming compact (mutates, returns the sketch).}
\item{\verb{$estimate()}}{Approximate number of distinct values seen.}
\item{\verb{$lower_bound(num_std_dev = 1)} / \verb{$upper_bound(num_std_dev = 1)}}{
Approximate confidence bounds on \code{estimate()}, at 1, 2, or 3 standard
deviations.}
\item{\verb{$lg_k()}, \verb{$seed()}, \verb{$theta()}, \verb{$num_retained()},
\verb{$is_empty()}, \verb{$is_estimation_mode()}, \verb{$is_ordered()},
\verb{$is_compact()}}{Metadata accessors.}
\item{\verb{$summary()}, \verb{$inspect()}, \verb{$serialize()}}{Structured metadata,
verbose debug output, and the native byte payload.}
}
}
\description{
Creates a \href{https://datasketches.apache.org/docs/Theta/ThetaSketches.html}{Theta}
sketch, a mergeable summary that estimates the number of distinct values
seen in a stream far larger than memory. Unlike \code{\link[=hll]{hll()}} and \code{\link[=cpc]{cpc()}}, Theta
sketches natively support set operations: \code{\link[=theta_union]{theta_union()}},
\code{\link[=theta_intersection]{theta_intersection()}}, \code{\link[=theta_difference]{theta_difference()}}, and \code{\link[=theta_jaccard]{theta_jaccard()}} combine
two sketches into a new result without mutating either input.
}
\details{
At most one of \code{x} or \code{bytes} may be supplied:
\itemize{
\item Pass \code{x} to build a sketch and immediately update it with a numeric or
character vector.
\item Pass \code{bytes} to reconstruct a sketch from a native serialized payload (as
produced by \code{sketch$serialize()}). The result is always a \emph{compact}
sketch (see below); \code{lg_k} must not be supplied alongside \code{bytes}.
Unlike \code{lg_k}, the hash \code{seed} is \emph{not} stored in the payload and must be
supplied if the original sketch did not use the default.
\item Pass neither for an empty (mutable) sketch with the given \code{lg_k} and
\code{seed}.
}

\code{update()} silently ignores \code{NA}/\code{NaN}/\code{NA_character_}, matching the
missing-value policy used across families; there is no \code{na_rm} argument.

A Theta sketch is either an \emph{update} sketch (mutable, \verb{$is_compact()} is
\code{FALSE}) or a \emph{compact} sketch (immutable, \verb{$is_compact()} is \code{TRUE}).
Fresh sketches built from \code{x}/\code{lg_k} are update sketches and can be grown
with \verb{$update()}. Compact sketches arise from \verb{bytes =} reconstruction,
\verb{$merge()}, or any of the \verb{theta_*()} set operations, and cannot be
updated further. \verb{$lg_k()} is only defined for update sketches.

Two sketches can only be merged with \verb{$merge()}, or combined with a
\verb{theta_*()} set operation, if they share the same \code{seed}; a mismatch
raises \code{datasketches_seed_mismatch}. \verb{$merge()} mutates the receiver into
a compact sketch holding the union of both inputs (so it can no longer be
\verb{$update()}d afterward).
}
\examples{
sketch <- theta(sample(1000, 5000, replace = TRUE))
sketch$estimate()
sketch$lower_bound()
sketch$upper_bound()

# Round-trip through the native byte format (always compact).
restored <- theta(bytes = sketch$serialize())
restored$is_compact()
identical(restored$estimate(), sketch$estimate())

# Set operations.
a <- theta(1:1000)
b <- theta(501:1500)
theta_union(a, b)$estimate()
theta_intersection(a, b)$estimate()
theta_difference(a, b)$estimate()
theta_jaccard(a, b)

}

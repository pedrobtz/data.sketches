% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cardinality-cpc.R
\name{cpc}
\alias{cpc}
\title{CPC sketch for approximate distinct counting}
\usage{
cpc(x = NULL, lg_k = NULL, seed = NULL, bytes = NULL)
}
\arguments{
\item{x}{Optional numeric or character vector to update the new sketch
with. Each element is hashed and contributes to the distinct-count
estimate.}

\item{lg_k}{log2 of the number of bins, a single whole number in
\verb{[4, 26]}. Larger \code{lg_k} is more accurate and larger. Defaults to \code{11}
(resolved when a fresh sketch is built). Must not be set when \code{bytes} is
supplied.}

\item{seed}{Hash seed, a single non-negative whole number up to \code{2^53}.
Defaults to \code{9001} (the upstream default), resolved whether or not \code{bytes}
is supplied.}

\item{bytes}{Optional \link{raw} vector holding a native serialized sketch to
reconstruct.}
}
\value{
A \code{cpc_sketch} object. Key methods:
\describe{
\item{\verb{$update(x)}}{Add numeric or character values (mutates, returns
the sketch).}
\item{\verb{$merge(other)}}{Absorb another sketch with the same \code{seed}
(mutates, returns the sketch).}
\item{\verb{$estimate()}}{Approximate number of distinct values seen.}
\item{\verb{$lower_bound(num_std_dev = 1)} / \verb{$upper_bound(num_std_dev = 1)}}{
Approximate confidence bounds on \code{estimate()}, at 1, 2, or 3 standard
deviations.}
\item{\verb{$lg_k()}, \verb{$seed()}, \verb{$is_empty()}}{Metadata accessors.}
\item{\verb{$summary()}, \verb{$inspect()}, \verb{$serialize()}}{Structured metadata,
verbose debug output, and the native byte payload.}
}
}
\description{
Creates a \href{https://datasketches.apache.org/docs/CPC/CpcSketches.html}{CPC}
(Compressed Probabilistic Counting) sketch, a very compact, mergeable
summary that estimates the number of distinct values seen in a stream far
larger than memory. CPC sketches are similar in purpose to \code{\link[=hll]{hll()}} but
serialize to a smaller payload, at the cost of slightly higher CPU use.
}
\details{
At most one of \code{x} or \code{bytes} may be supplied:
\itemize{
\item Pass \code{x} to build a sketch and immediately update it with a numeric or
character vector.
\item Pass \code{bytes} to reconstruct a sketch from a native serialized payload (as
produced by \code{sketch$serialize()}). \code{lg_k} is restored from the payload
and must not be supplied alongside \code{bytes}. Unlike \code{lg_k}, the hash
\code{seed} is \emph{not} stored in the payload and must be supplied if the
original sketch did not use the default.
\item Pass neither for an empty sketch with the given \code{lg_k} and \code{seed}.
}

\code{update()} silently ignores \code{NA}/\code{NaN}/\code{NA_character_}, matching the
missing-value policy used across families; there is no \code{na_rm} argument.

Two sketches can only be merged with \verb{$merge()} if they share the same
\code{seed}; a mismatch raises \code{datasketches_seed_mismatch}.
}
\examples{
sketch <- cpc(sample(1000, 5000, replace = TRUE))
sketch$estimate()
sketch$lower_bound()
sketch$upper_bound()

# Round-trip through the native byte format.
restored <- cpc(bytes = sketch$serialize())
identical(restored$estimate(), sketch$estimate())

}

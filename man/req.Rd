% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quantiles-req.R
\name{req}
\alias{req}
\title{REQ sketch for relative-error approximate quantiles of a numeric stream}
\usage{
req(x = NULL, k = NULL, hra = NULL, bytes = NULL)
}
\arguments{
\item{x}{Optional numeric vector to update the new sketch with.}

\item{k}{Sketch width controlling the accuracy/size trade-off, a single
even whole number in \verb{[4, 1024]}. Larger \code{k} is more accurate and larger;
\code{k = 12} corresponds to roughly 1\% relative error at 95\% confidence.
Defaults to \code{12} (resolved when a fresh sketch is built). Must not be set
when \code{bytes} is supplied.}

\item{hra}{If \code{TRUE}, prioritize accuracy for high ranks (near 1.0); if
\code{FALSE}, prioritize low ranks (near 0.0). Defaults to \code{TRUE} (resolved
when a fresh sketch is built). Must not be set when \code{bytes} is supplied.}

\item{bytes}{Optional \link{raw} vector holding a native serialized sketch to
reconstruct.}
}
\value{
A \code{req_sketch} object. Key methods:
\describe{
\item{\verb{$update(x)}}{Add numeric values (mutates, returns the sketch).}
\item{\verb{$merge(other)}}{Absorb another sketch (mutates, returns the sketch).}
\item{\verb{$quantile(probs, inclusive = TRUE)}}{Approximate quantiles for
probabilities in \verb{[0, 1]}.}
\item{\verb{$rank(x, inclusive = TRUE)}}{Approximate ranks of \code{x}; missing
inputs return \code{NA}.}
\item{\verb{$cdf(split_points)} / \verb{$pmf(split_points)}}{Cumulative / mass
estimates; return \code{length(split_points) + 1} values.}
\item{\verb{$rank_lower_bound(probs, num_std_dev = 1)} /
\verb{$rank_upper_bound(probs, num_std_dev = 1)}}{Approximate confidence
bounds on the rank(s) \code{probs}, at 1, 2, or 3 standard deviations.}
\item{\verb{$n()}, \verb{$k()}, \verb{$num_retained()}, \verb{$is_empty()},
\verb{$is_estimation_mode()}, \verb{$min()}, \verb{$max()}, \verb{$is_hra()}}{
Metadata accessors.}
\item{\verb{$summary()}, \verb{$inspect()}, \verb{$serialize()}}{Structured metadata,
verbose debug output, and the native byte payload.}
}
}
\description{
Creates a \href{https://datasketches.apache.org/docs/REQ/ReqSketch.html}{REQ}
(Relative Error Quantiles) sketch over \code{double} values. Like \code{\link[=kll_doubles]{kll_doubles()}},
a REQ sketch is a compact, mergeable summary that answers approximate
quantile, rank, CDF, and PMF queries over a stream far larger than memory.
Unlike KLL, REQ's accuracy is \emph{relative} and rank-dependent: error is small
near the prioritized end of the rank range (controlled by \code{hra}) and grows
towards the other end.
}
\details{
At most one of \code{x} or \code{bytes} may be supplied:
\itemize{
\item Pass \code{x} to build a sketch and immediately update it with a numeric vector.
\item Pass \code{bytes} to reconstruct a sketch from a native serialized payload (as
produced by \code{sketch$serialize()}). Width and \code{hra} are restored from the
payload, so \code{k} and \code{hra} must not be supplied alongside \code{bytes}.
\item Pass neither for an empty sketch of width \code{k}.
}

\code{update()} silently ignores \code{NA}/\code{NaN}, matching the upstream/Python behaviour;
there is no \code{na_rm} argument.
}
\examples{
sketch <- req(rnorm(10000))
sketch$quantile(c(0.25, 0.5, 0.75))
sketch$rank(c(-1, 0, 1))
sketch$rank_upper_bound(0.99)

# Round-trip through the native byte format.
restored <- req(bytes = sketch$serialize())
identical(restored$quantile(0.5), sketch$quantile(0.5))

}
